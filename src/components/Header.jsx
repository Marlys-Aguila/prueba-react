
import logo from "../img/logo.png";

const Header = () => {
    return (
        <header className='center'>
            <img src={logo} alt='Logo de Breaking Bad' className='logo'></img>
        </header>
    );
};

export default Header;
